.venv/
__pycache__/
*.pyc
.DS_Store

# Все локальные данные, секреты, логи, метки, журнал — на сервер не нужны,
# в публичный репо тем более. Конфиги с токенами тоже здесь.
data/

# Модели: тяжёлый pkl не коммитим, но лёгкая JSON-модель нужна серверу.
models/*
!models/imitation.json
